#include "minishell.h"

check_if_builtins(t_node *node)		// input is a node?
{
	char *cmd_name;
	
	cmd_name = node->tokens[i][0];	// IS tokens[i][0] the correct PATH to the name



	/*
		THE FOLLOWING FUNCTIONALITY IS MISSING:

		Example:
		cd core | grep a


	*/
}